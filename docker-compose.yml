version: '3.3'

services:
    zen:
        build:
            args:
                CBF_VERSION: "${CBF_VERSION:-}"
                CONTAINER_NAME: zen
                FROM_BASE: ${DOCKER_REGISTRY:-}php7:${BASE_TAG:-latest}
            cache_from:
                - ${DOCKER_REGISTRY:-}alpine:3.8
                - ${DOCKER_REGISTRY:-}base_container:${BASE_TAG:-latest}
                - ${DOCKER_REGISTRY:-}supervisord:${BASE_TAG:-latest}
                - ${DOCKER_REGISTRY:-}php7:${BASE_TAG:-latest}
            context: .
            dockerfile: Dockerfile
            labels:
                container.fingerprint: $CONTAINER_FINGERPRINT
                container.git.branch: $CONTAINER_GIT_BRANCH
                container.git.commit: $CONTAINER_GIT_COMMIT
                container.git.url: $CONTAINER_GIT_URL
                container.origin: $CONTAINER_ORIGIN
                container.original.name: zen
                container.version: ${CONTAINER_VERSION:-3.0.0}
        container_name: zen
        environment:
            DEBUG_TRACE: ${DEBUG_TRACE:-0}
            SUPERVISORD_PASS: $CFG_PASS
            SUPERVISORD_USER: $CFG_USER
            ZEN_HOST: mysql
            ZEN_PASS: $CFG_PASS
            ZEN_USER: $CFG_USER
        hostname: zen
        image: ${DOCKER_REGISTRY:-}zenphoto:${CONTAINER_TAG:-latest}
#        links:
#            - mysql
        logging:
            driver: json-file
            options:
                max-file: "3"
                max-size: "10m"
        ports:
            - 8004:80
#        restart: unless-stopped
        volumes:
            - ./vols/log:/var/log
            - ./vols/photos:/var/www/albums
